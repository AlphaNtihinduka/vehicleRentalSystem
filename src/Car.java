// Inherit the vehicle class
// Implement the Car subclass.
public class Car extends Vehicle{
    // Create a Car constructor and use the Vehicle attributes
    public Car(String licensePlate, String colour, double price_per_day) {
        super(licensePlate, colour, price_per_day);
    }
}
