
// Inherit the vehicle class
// Implement the Truck subclass.
public class Truck extends Vehicle{

    // Create a Truck constructor and use the Vehicle attributes
    public Truck(String licensePlate, String colour, double price_per_day) {
        super(licensePlate, colour, price_per_day);
    }
}